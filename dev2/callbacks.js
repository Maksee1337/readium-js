function updateSelectorsValues(data) {    // alert('xxx')    $("#selectFontFamily").empty();    $("#selectChapter").empty();    $("#selectPage").empty();    for(let i = 0 ; i < data.fonts.length ; i++) {        var o = new Option(`${data.fonts[i].displayName}`, `${data.fonts[i].fontFamily}`);        $("#selectFontFamily").append(o);    }    for(let i = 0 ; i < data.pagesCount ; i++) {        var o = new Option(`${i+1}`, `${i+1}`);        $("#selectPage").append(o);    }    for(let i = 0 ; i < data.TOC.length ; i++) {        var o = new Option(data.TOC[i].title, data.TOC[i].idref);        $("#selectChapter").append(o);    }}function updateInformation(data) {    const info = maneno.getBookInfo();     let message = `Pages in book(${info.type}): ${info.pagesCount}. Current page: ${data.selectedPageIndex.left}`;     if(data.selectedPageIndex.right){         message += ` - ${data.selectedPageIndex.right}`;     }    $("#selectPage").val(data.selectedPageIndex.left);    $("#selectChapter").val(data.selectedChapterValue);    $("#selectFontFamily").val(data.selectedFontFamily);    $("#selectFontSize").val(data.selectedFontSize);    $(`#info1`).text(message);}